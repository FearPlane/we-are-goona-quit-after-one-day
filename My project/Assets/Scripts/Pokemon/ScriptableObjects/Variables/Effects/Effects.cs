namespace PokemonData.Effects
{
    public class Effects
    {
        public Type type; //The kind of effect
        public Status status; //Which status condition to apply (only for statusCondition)
        
    }
}